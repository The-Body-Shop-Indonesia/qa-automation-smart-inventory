describe('General API Test Group', () => {
  it('Shows My voucher yg sudah direedem', () => {})
  it('Shows My voucher yg sudah dipake', () => {})
  it('Shows My voucher yg sudah expired, harusnya masuk ke tab Pas Voucher', () => {})

  // ---- technical sections
  // wrong ID format: non-MongoDB, undefined, empty
  it('', () => {})

  // not found ID
  it('', () => {})

  // ---- business sections
  // what data to show?
  it('', () => {})

  // is the price right?
  it('', () => {})

  // is redeemable? compared to my current point
  it('', () => {})

  it('', () => {})
  it('', () => {})
})
