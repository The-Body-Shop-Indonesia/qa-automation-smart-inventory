describe('General API Test Group', () => {
  it('Shows Voucher Detail', () => {})

  // ---- technical sections
  // wrong ID format: non-MongoDB, undefined, empty
  it('', () => {})

  // not found ID
  it('', () => {})

  // ---- business sections
  // what data to show?
  it('', () => {})

  // is the price right?
  it('', () => {})

  // is redeemable? compared to my current point
  it('', () => {})

  it('', () => {})
  it('', () => {})
})
